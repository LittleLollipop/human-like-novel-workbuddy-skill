---
name: human-like-novel
description: 仿人类小说创作技能，从根源上解决AI生成痕迹问题（重复描写、节奏单一、用词单调、精确数字）。通过情绪词库、设定点范例库、章节三要素计划、负面约束系统，让AI写出具有"人感"的小说。内置轻量级图数据设定真源方案，以知识图谱解决长篇小说长期一致性。
description_zh: "仿人类小说创作，根治AI生成痕迹，让AI写出有人感的小说；内置轻量级图数据设定真源方案"
description_en: "Human-like novel writing skill that eliminates AI fingerprints from generated text; built-in lightweight graph-data source-of-truth for long-form consistency"
version: 2.6.6
display_name: "仿人类小说创作"
display_name_en: "Human-Like Novel Writing"
visibility: "public"
---

# 仿人类小说创作

从根源上解决AI生成痕迹问题，让AI写出具有"人感"的小说。

## 问题诊断

AI生成文本有明显痕迹的四大原因：

1. **大量重复描写**：AI倾向于反复强调同一个点（刻意点题）
2. **节奏高度统一**：段落长度、句式分布过于均匀，没有变化
3. **用词单调**：高频词集中，缺乏多样性，情绪与用词脱节
4. **过于精确的数字**：AI习惯使用精确数字（37秒、142步），人类更常用模糊表达
5. **语体迁移 / 信息密度偏好（#121）**：模型倾向「最经济」表达——单字承载多义（古文特性），于是正文冒出非古非白的缩词（岗顶/尔等/须臾/自语）。风格层约束（文风/角色用词）压不住词法层的单字选择，必须词法层硬约束 + 出口机检。详见下节「白话护栏」。

本技能从**根源**上解决这些问题，而不是在生成后修补。

---

## 白话护栏（Baihua Guard，2026-08-25 新增，开写必读）

**问题**：现代白话口语的特征是**冗余**（双音节词、语气词、口头禅、把话说满）；模型的默认倾向却是**信息密度优先**（单字多义、古文压缩），导致正文出现「不像口语不像古文」的缩词（岗顶/尔等/须臾/自语/莫慌）。

**硬规则（写作时逐条对照）**：
1. 叙事与对话一律现代白话，**双音节词为主**；「口语的冗余是特征，不是缺陷——宁可说满，不可压缩」。
2. 对话内禁文言单字/文言词（尔/汝/吾/乃/遂/须/勿/莫/颇/甚/曰/即/须臾/尔等/何故…）——角色说话像念文言=不合格。
3. 叙事禁半文半白缩词（岗顶→山岗顶上、自语→自言自语、行至→走到、回眸→回头…），拿不准就写双音节口语。
4. **例外**：①书名号《》内专名（歌名/作品名）豁免；②明确修辞意图可破例（老学究角色/引用谚语/歌词唱词），但须有意识。
5. 现代口语里本来就在用的词不要误杀：甚至/然而/偶尔/莫非/必须/其余/剩下/自言自语/半晌（北方口语）等。

**机器校验**：机检脚本读 `references/baihua-lexicon.json`（词表 190+ 词，可累积——踩到新词往表里加并记项目日志）。分级：对话内命中=**硬错**（机检不过）；叙事命中=**提示**（给替换建议）；`dialogue_only` 单字词仅查对话，`safe_combos` 排除合法长词（自言自语/山岗顶上/食言/绰绰有余…）防误伤。词表放技能层（普适跨书），项目脚本 `tools/check_chapter.py` 直接引用。

> **实战经验库**：上述四类问题在真实项目中常以「系统性文风病」形态出现（如信息空转、章尾抒情拖尾、设定/专名前后矛盾）。这些**已踩过的真实坑**已拆分收录在 `references/error-cases/`（入口 `_index.md` 一眼看全当前全部条目（编号至 #121）+ 按主题拆分的 `cat-*.md` 详情 + 机器层 `_rules.json`/`check_error_cases.py`），每条含「现象→根因→错误写法→正确写法→可固化规则」。查章时先读 `_index.md`，再跑 `check_error_cases.py <章节>` 自动扫通用机检项（#020/#023/#024），创作前对照规避，遇到新问题往 `_template.md` 追加新案例并同步索引。**修复/改稿时**另查 `references/ai-flavor-words.md`（AI 味用词句式负例清单，从 error-cases 拆分的快速负例，修复前过一遍、修复后扫改过的位置）。

---

## 可选模块：对话式设定构建（开书前）

长篇连载的逻辑错误、前后矛盾，根因常在**设定库从源头欠账**（量级不足 + 边写边补）。本技能提供可选前置模块：

- **启用**：长篇（目标 ≥50 万字）、需要完整世界观——开书前用对话式引导，从作者脑中结构化「问」出设定库（七阶段：书核/世界观/人物/能力/剧情/专名口径/平台红线）
- **跳过**：短篇、快节奏开书、偏好边写边定——不启用不影响主流程
- **达标线**：设定集 ≥ 目标字数 × 1/10（低于 1/20 视为严重欠账）
- 详见 `references/setting-builder.md`

> 设定权始终在作者：模块只负责「问」和「整理」，所有 canon 由作者拍板；纯对话 + 落盘实现，不依赖平台特性。

---

## 核心架构

```
novel-project/
├── settings/                    # 设定档案
│   ├── world.md                 # 世界观总览
│   ├── characters/              # 角色设定
│   │   ├── <角色名>.md
│   │   └── <角色名>-emotion.md  # 情绪-用词映射表（人感核心）
│   ├── geography/               # 地理/势力
│   ├── rules.md                 # 世界规则
│   └── examples/                # 设定点写作范例库
│       ├── <设定点名>.md         # 每个主要设定点的多角度范例
│       └── ...
├── chapters/                    # 章节档案
│   ├── outline.md               # 大纲
│   ├── plans/                   # 章节创作计划（文件式；图库模式则不写本目录，plan 存图库 plan_chXXX 节点）
│   │   ├── ch-XXX-plan.md       # 每章的三要素计划
│   │   └── ...
│   └── arc-N/                   # 分卷正文
│       └── chapter-XXX.txt
├── tracker/                     # 追踪器
│   ├── foreshadowing.md         # 伏笔追踪
│   ├── conflicts.md             # 冲突记录
│   └── feedback.md              # 读者反馈
└── state.json                    # 项目状态
```

---

## 目标平台/读者调性（风格基准，开书前必选）

**调性是风格基准，不是修饰选项**。同一故事，写给高注意力平台与低注意力平台，是两套写法，甚至是两套流程。番茄类网文读者注意力贵，描写少、直给；文学/史诗作品允许铺陈。写前不定调性，正文必然"两头不靠"。

### 两种调性基准

**直给型（高注意力平台 / 网文快餐，如番茄）**
- 读者注意力贵：**每段必须推进**（剧情/人设/笑点/爽点），不设"氛围段"
- 描写极简：环境/外貌/动作只给**钩子性特征**（一两笔），不铺陈
- **对话密度高（≥50%）**、短句多、情绪直给、不绕弯
- 笑点/爽点**不被描写稀释**（注意力密集，每屏一个）
- 内心戏压成**短判断**（一两句），不展开推演
- 特征诊断：读起来"节奏慢/不好笑"往往不是情节慢，是**文本注意力分散**——描写和过渡稀释了笑点

**铺陈型（低注意力平台 / 文学 / 史诗）**
- 允许氛围铺垫、环境映射、隐喻、慢节奏
- 描写可展开，内心戏可推进
- 对话占比可低至 30%

### 影响范围（两套流程）

| 环节 | 直给型 | 铺陈型 |
|------|--------|--------|
| 章节计划 | 三要素压短（每要素一行约束，够用就停） | 三要素完整（含耦合检查/多轮迭代） |
| 行文风格（要素①） | 对话≥50%、描写≤15%、每段推进；长段≤1处 | 文学节奏，允许长段氛围 |
| 创作重点（要素②） | 剧情推进+喜剧/爽点优先，描写靠后 | 可偏氛围/内心 |
| 机检 | 加注意力检查（长段无对话计数、描写占比、无推进段提示） | 常规 |
| 写后自查 | 每段自问"推进了什么？删掉会不会更好？" | 常规 |

### 使用

1. **开书前确定调性**（setting-builder 或首个 plan 前），写入书核/设定库
2. 每章 plan「要素①行文风格」**标注调性**（直给/铺陈），按对应基准写
3. 直给型写作时按「每段推进」自查；描写段若 2 句内不落回动作/对话/笑点，即删
4. 机检规则：直给型项目在项目级脚本（如 check_chapter.py）加注意力统计（长段无对话段落数、对话占比），非直给型可不开

---

## 工作流程

### 阶段零：长篇分层规划（长篇小说必做，error-case #109）

> **为什么必做**：长篇反复确认"目标字数/卷数/每卷章数/剧情分配"后，一写正文仍严重偏离（单元写一章就闭环、容量差 10 倍）。根因=宏观数字确认没有约束力，**只有逐章内容梗概锁定后才不会偏**。

**步骤 A：确定分层（卷→单元→章）**
- **卷**（约 50-70 万字）：一个完整大弧（开篇/中段/高潮/收束），含 10-15 个单元
- **单元/剧情弧**（约 10-12 章）：一个连续事件群（多个小事件链式推进+钩子衔接），**不是单事件**——单事件最多 1-2 章
- **章**（2800-3800 字）：一个完整场景/事件节拍；**低于 2800 视为内容量不足信号**（回查 plan 是否该并章，禁硬灌水）

**步骤 B：逐章内容梗概（本章核心，动笔前必须完成）**
- 整卷每章一条梗概：**章号 + 一句话（本章事件 + 结尾钩子 + 所属单元）**
- 梗概连续锁定：ch001→chN 顺序写全，单元内事件链式递进（A→B→C），单元间留钩子
- 容量校验以逐章梗概为准：卷目标字数 ÷ 每章字数 = 章数 → 逐章分配完毕才算数（宏观数字问答不算数）

**步骤 C：防偏离自检（每次动笔前跑一遍）**
- 单元内是否 ≥6 章？若一个"单元事件"写 1-2 章就闭环 → 该单元事件粒度太大或事件池不够，回步骤 B 重排（把单事件拆成事件群）
- 本章梗概是否与前后章连续（钩子链不断）？
- 每章字数与卷目标字数的累计进度是否对得上（写 1/10 章节时应完成约 1/10 容量）？
- **首稿字数阈值（硬规则）**：正文首稿若低于 **3000 字** → **第一时间报告异常**（这是"本章内容量不足"的信号，不是"该硬填字数"），必须回查 plan：笑点是否重复/信息获取型过多/场景太少 → **优先压缩合并章节**（相邻章并章、重复事件合并），**禁止硬灌水凑字数**（注水=撞 canon 冲突+稀释节奏）。字数缺口是结构信号，不是填充任务。
- **大笑点密度（硬规则，2026-08-17 用户定）**：**每 2 章至少 1 个「剧情层面的大笑点」**（大笑点=可被读者当场面复述的重场戏：设局整人成功/对手当众出丑/规则钻空反超/大场面比试/机制梗爆发；区别于单句梗、吐槽、谐音）。规划时逐章标注大笑点位置（哪个笑点是剧情级），**连续 2 章无大笑点 → 前后压缩合并**（把大笑点集中），**整卷预期字数的缺口用「新增单元」补**（不靠注水、不靠拖章节）。判据：本章是否有 1 个能写进"这本书最搞笑场面"清单的桥段？
- **若设定/剧情用图数据库管（卷→单元→章分层落图）**：写**查询驱动的自动校验器**跑一致性（禁全量 dump 人工比对，图库价值=边定向遍历+字段级提取+只输出差异）。规则模板：
  - 章→单元映射：chXXX 的 unit 出边 vs 总规划（如 volume1_chapter_map）的章范围
  - plan 衔接链：plan 的【衔接】→ 下一章（目标章存在）
  - 角色登场：chr 的【登场】章 vs 被 ch 引用的最小章（不得早于登场）
  - 时间线衔接词：plan 时间线里"衔接chXXX"=与上一章衔接（语义统一，forward 归【衔接】字段）
  - **剧情重构后必须全层同步**（总规划/单元/ch 锚点/plan 四层一起更新），重构完跑校验器兜底

### 阶段零·五：层级完整性与逐节写作（2026-08-25 用户拍板，长文必读）

> **核心修正**：结构完整性 ≠ 章节完整性。**章节不是故事的单位**——单章可以没有完整结构，完整性向上查必有锚点；且长文一次性生成质量递减（开头好、越往后越差），**一次只写一节**。

**① 四级层级（卷→段→章→节），图库落边 + structure 属性**
- `volume`（卷）：完整大弧（开篇/中段/高潮/收束），structure=complete
- `segment`（段/弧线）：一个完整事件组（设局→推进→爆发→余波，跨 3-8 章），structure=complete（段=**最常用完整性锚点**）
- `chapter`（章）：弧线上的节拍（铺陈/升级/转折/高潮/余波之一），structure=complete|partial|open——**单章不要求闭环**
- `beat`（节）：一次写作的产出单位（≈500-900 字，=plan 分段梗概的一个段落），不要求完整
- 图库：`contains` 边连所属关系（volume→segment→chapter→beat）；每节点带 `structure` 字段

**② 完整性锚点规则（写任何节点前必查）**
- 向上遍历祖先链，找**最近 `structure=complete` 的节点**——它的弧线就是本节点的方向与闭环锚点
- 单章/单节「不完整」完全合法（开头章不强行收束、中间章留白），**闭环责任在最近 complete 层**（段末章负责收弧）
- 验收：段/卷末查弧线闭合；单章只验收「推进了弧线 + 阅读体验不碎（钩子/笑点/悬念密度够）」
- 判据一句话：**「这一章没闭环」不是问题，「这个段没闭环」才是问题**

**③ 一次写一节（生成质量衰减对策）**
- 节 = plan 分段梗概的一个段落（章 plan 的段落1-5 即 5 节）
- 流程：写节1 → 节级自检（推进/笑点/白话/衔接）→ 写节2（**先回读上节末句作唯一锚点**）→ … → 节N → **章级机检**（字数/破折号/引号/说·道等章级约束）→ selfcheck
- 每节都是「新开头」，避免长程生成质量衰减；节间靠上节末句锚点衔接，不靠记忆
- 章内节数 = plan 段落数（通常 5 节/章）；节级不单独跑字数机检（字数按章）

**④ 字数不足 = 回 plan 加节，禁止正文内扩写（2026-08-26 用户拍板）**
- **错误做法**：章级机检报字数不足 → 直接在正文基础上「补几段对话」扩写——这是给已有段落**注水**，破坏节奏/逻辑/结构，且容易产出为凑字数的废话
- **正确做法**：字数不足说明 **plan 的节数/容量不够**（不是正文写得不够）——
  1. **回退到 plan 层面**：给 plan【分段梗概】增加新段落（段落6、7…），内容是**新推进/新场景/新笑点/新视角**（不是已有段落的展开）
  2. **同步更新图库** `plan_chXXX` 节点（新增段落梗概）
  3. **写新节追加到正文**（一次写一节流程：回读上节末句锚点 → 写新节 → 节级自检）
- 判据：**「本章信息量不足」才加节；「本章信息够、只是表达啰嗦」则删减，方向相反**——加节的前提是 plan 里还有没落地的新内容可挖，没有就说明 plan 本身要加料（段/卷层补内容）

### 阶段一：设定构建（正文创作前必须完成）

#### 步骤1：初始化项目

创建项目结构，加载模板。

**目录结构**：
- `settings/`：世界观、角色、地理、规则设定
- `settings/examples/`：**设定点写作范例库**（AI痕迹根治核心）
- `settings/characters/`：角色设定 + **情绪词库**
- `chapters/plans/`：**章节创作计划**（AI痕迹根治核心；**图库模式：plan 存图库 `plan_chXXX` 节点，不写 md**，见「图库 plan 字段清单」）
- `tracker/`：伏笔、冲突、反馈追踪

#### 步骤2：录入设定 + 构建写作范例库

**2.1 基础设定录入**

支持的类型：`world`（世界观）/ `character`（角色）/ `rule`（规则）/ `geography`（地理）

**2.2 构建设定点写作范例库（核心）**

每个主要设定点必须提供 **3-5个** 不同角度的写作范例。

**为什么需要范例库？**
AI最大的问题之一是"直白点题"——反复用相同的方式强调同一个主题。范例库提供多种不同的点题方式，AI从库中选一个改编使用，**禁止自创点题方式，禁止重复使用同一范例**。

**范例角度要求**（至少3种）：
- 直接描写
- 对话体现
- 内心独白
- 环境映射
- 他人视角

**范例文件格式**（`settings/examples/<设定点名>.md`）：

```markdown
# 设定点：<名称>

## 角度一：直接描写
<150字以内的写作范例>

## 角度二：对话体现
<150字以内的写作范例>

## 角度三：内心独白
<150字以内的写作范例>

## 角度四：环境映射
<150字以内的写作范例>

## 角度五：他人视角
<150字以内的写作范例>
```

**使用规则**：
- 正文创作时如需"点题"，**必须从范例库中选取一个范例进行改编**
- 禁止直白重复范例原文
- 禁止全文重复使用同一范例（每个范例最多用2次，且必须间隔至少10章）

#### 步骤3：构建角色情绪词库（人感核心）

**为什么需要情绪词库？**

AI写作"平淡"的根本原因是：**用词没有情绪偏向**——更准确地说，是**情绪没有通过角色的声口/视角进入措辞**。

人类作者让情绪"可被感知"的方式，不是 tell（她感到高兴），也不是把情绪翻译成身体反应（心口沉/肩膀拧紧/脊梁像旧门轴——那恰是 AI 最典型的平感破绽，见 #074），而是**让角色用其固定的视角，为同一个事物命名、选动词、定褒贬**。同一桩事，措辞一换，感受全变：

- 经典例：**奉天子以令不臣**（尊）vs **挟天子以令诸侯**（僭）——事实相同，只换了动词（奉/挟）与对象定位，情绪与道德重量截然不同。
- 愤怒时 → 情绪在"他砸了碗"的动词里，不在"他心口一沉"里（动词：砸、撕、掐；形容词：尖锐、滚烫）
- 自卑时 → 用词懦弱（动词：缩、躲、低声；形容词：灰暗、微小）
- 悲伤时 → 用词沉重（动词：瘫、滑、闭；句式：长句、停顿、省略号）

**检验法**：写完一段，遮住"他说/她想"，只看描写事物的名词与动词——能否读出角色此刻的情绪？能 → 声口携带成功；不能（只能靠"心口沉/眼眶热"补情绪）→ 重写为声口措辞（原理见 #125）。

情绪词库是用词人感的核心工具。

**文件格式**（`settings/characters/<角色名>-emotion.md`）：

```markdown
# 角色：<名字>

## 基本信息
- 身份/背景：<角色身份，如"克隆人战士"、"古代剑修">
- 教育背景：<角色受过什么教育，如"自学古典文学"、"只接受过军事训练">
- 性格特征：<核心性格>

## 情绪状态 → 用词特征映射

### 愤怒
- 动词倾向：砸、撕、掐、吼、跺
- 形容词倾向：尖锐、滚烫、刺耳、猩红
- 句式倾向：短句、断句、反问句
- 禁忌：平静叙述、长篇解释、委婉表达

### 自卑/怯懦
- 动词倾向：缩、躲、低声、后退、垂下
- 形容词倾向：灰暗、微小、模糊、瑟缩
- 句式倾向：不确定语气（"可能"/"也许"）、被动句
- 禁忌：果断决定、主动出击的描写

### 悲伤/绝望
- 动词倾向：瘫、滑、闭、颤抖
- 形容词倾向：冰冷、空洞、沉重、灰白
- 句式倾向：长句、停顿、省略号
- 禁忌：轻快节奏、明亮词汇

### 兴奋/喜悦
- 动词倾向：跳、冲、抓、亮
- 形容词倾向：滚烫、耀眼、清脆、鲜活
- 句式倾向：短句、感叹句、排比
- 禁忌：冷静分析、负面词汇

### 冷静/算计
- 动词倾向：敲、扫、计算、排列
- 形容词倾向：清晰、冰冷、精确、有序
- 句式倾向：陈述句、逻辑连接词
- 禁忌：情绪化表达、夸张修辞

### 紧张/恐惧
- 动词倾向：吞、缩、僵、抖
- 形容词倾向：冰冷、尖锐、狭窄
- 句式倾向：短句、断句、重复词
- 禁忌：从容不迫的描写

### 疲惫/麻木
- 动词倾向：垂、拖、靠、闭
- 形容词倾向：灰、沉、钝、慢
- 句式倾向：长句、缓慢节奏
- 禁忌：激烈动作、高昂情绪

## 引用偏好（旁征博引的人感来源）

**知识背景**：
- <角色受过什么教育，读过什么书，熟悉什么领域的知识>
- 例：零的记忆碎片中有古典文学片段，但不完整

**引用风格**：
- <角色倾向于用什么方式引用，如"化用意境"、"直接引用"、"暗喻">
- 例：零倾向用简洁有力的句子，不拖泥带水

**适合引用的场景**：
- <什么情况下这个角色会引用，如"哲学思辨时"、"面临抉择时">
- 例：零在思考存在意义时会联想到相关名言

**不适合引用的场景**：
- <什么情况下这个角色不会引用，如"战斗时"、"日常对话">
- 例：零在激烈战斗时不会引用，不符合性格

**引用内容偏好**：
- 适合：<角色会引用的内容类型，如"存在主义、反抗、身份认知相关">
- 不适合：<角色不会引用的内容类型，如"过于文绉绉的诗词"、"自己不懂的内容">
- 例：零适合引用简洁有力的存在主义名言，不适合引用婉约派诗词

**引用素材库**（2026-08-25 升级：从「搜索关键词」改为「现成句子」，写角色时即列 10-20 条，plan 时从库中选）：
- 俗语/老话：<可直接引用的句子，如"老话说得好，光说不练假把式">
- 名言/诗词：<角色真懂或半懂的名句（半懂=引错是笑点）>
- 歌词/作品：<角色熟悉的歌词/作品片段（吟游诗人/追星族类角色专属）>
- 行业黑话/典故：<职业相关引用（商人引行话、老江湖引江湖典故）>
- 网络热梗：<现代角色/玩家可用（Z世代/深度冲浪/偶尔刷刷）>
- ...（根据角色经历和性格扩展；踩到好句补进来，开放累积）

## 流行文化熟悉度（非严肃文学适用）

**互联网使用习惯**：
- 是否熟悉网络热梗：是/否
- 熟悉程度：深度冲浪 / 偶尔刷刷 / 完全不懂
- 年龄层：Z世代 / 千禧 / 中年 / 老年（影响梗的使用）

**引用风格偏好**：
- 适合：网络热梗、流行语、梗图描述、二次元引用
- 不适合：过于严肃的场合、与角色身份不符的场景
- 使用频率：高频（每章多次）/ 中频（每章1-2次）/ 低频（偶尔）

**搜索关键词库**（创作时用来搜索热梗的关键词）：
- 幽默场景：最近流行搞笑梗、网络热梗
- 无奈场景：无奈表情包、网络流行语
- 震惊场景：震惊梗、网络流行语
- ...（根据角色性格扩展；**热梗侧同样优先用现成素材**——plan 引用素材位直接给句子，不靠"搜索方向"）

**使用规则**：
- 必须符合角色身份（古代角色不会用现代梗）
- 必须符合场景氛围（悲伤场景不适合搞笑梗）
- 必须自然（不能为了玩梗而玩梗）
```

## 声口特征（角色对话个性化的核心）

**为什么需要声口特征？**

AI 写作最隐蔽的痕迹之一是**所有角色对话共享同一个叙事语感**——角色A和角色B的句子长度、措辞习惯、口语节奏完全一致。人类不会这样说话：工程师和伦理顾问、丧偶的男人和丧女的母亲，不可能措辞相同。

声口特征强制每个角色的**对话**拥有独立的说话风格。与"情绪→用词映射"不同：那节控制**叙述**，本节控制**对话台词**。

**文件格式**（在 `characters/<角色名>-emotion.md` 中追加此节）：

```markdown
## 声口特征（影响对话措辞，非叙述）

### 基础声口
- **句子倾向**：[短句碎片 / 完整长句 / 混合]
  - 短句碎片（<10字）：话少、不擅表达、技术脑
  - 完整长句：习惯分析、职业话多、有准备
  - 混合：日常型，随情绪切换
- **口头习惯**：
  - 常用词/句式（如"你告诉我""就是""那个……"）
  - 停顿方式（吞后半句 / 用省略号 / 不打断）
- **称呼习惯**：怎么叫别人、怎么叫自己

### 情绪→台词变化

| 情绪 | 句子怎么变 | 措辞怎么变 | 节奏怎么变 |
|------|-----------|-----------|-----------|
| 愤怒 | 更短、更碎 | 攻击性词、反问 | 快、打断式 |
| 悲伤 | 变短、吞字 | 虚词多、省略 | 慢、停顿多 |
| 冷静/专业 | 回基准 | 术语、完整句 | 匀速 |
| 疲惫 | 更短、含糊 | 口头禅多、懒得措辞 | 拖、重复 |
| 紧张 | 断句、改口 | 不确定词 | 急、结巴感 |

### 禁忌
- 这个角色**绝对不用的措辞方式**：
  - 例：沈确（工程师）不用反问句、不用长串形容词

### 声口对比（必填，写对话时对照）
- 角色A vs 角色B 的核心差异（一句话）：
  - 例：沈确 短、不解释、技术名词 / 苏蔓 反问、案例、累了就变短
```

**使用规则**：
1. 每个主要角色必须建立情绪词库
2. 每章创作前，明确每个角色在本章的情绪状态
3. 创作时，对照情绪词库选择用词和句式
4. 角色情绪变化时，用词必须同步变化
5. 遇到引用触发点时，对照"引用偏好"搜索合适内容
6. 遇到流行文化引用触发点时，对照"流行文化熟悉度"搜索合适内容
7. 每个主要角色必须填写声口特征（基础声口 + 情绪→台词变化 + 声口对比）
8. 写对话前，先查当前角色的声口特征（句子倾向、口头习惯、当前情绪→措辞）
9. 写完对话后做"声口盲测"：拿掉角色名，能看出是谁在说话？能 → 通过；不能 → 重写台词
10. 两个角色对话时严格对照"声口对比"：一个说短句另一个就该说长的，一个用术语另一个用大白话，不可趋同

---

### 阶段一·五：设定-情节耦合检查（正文创作前必须 pass）

**为什么需要这个阶段？**

AI生成的小说往往"设定和故事脱节"——设定只是壳，故事换到任何背景都能成立。人类作家在构思时会反复推敲："这个设定真的驱动了故事吗？"

本阶段强制AI在写正文前，先证明"设定值得被写进故事里"。

---

#### 步骤3.5：设定必要性检查（通用）

**检查核心问题**：

```
问：如果去掉这个核心设定，故事还能成立吗？

→ 能成立 = ❌ 耦合失败，设定是壳
  - 例：男女主角只是恰好是科学家，换到任何职业，爱情故事都一样
  - 修正：让科学设定直接制造冲突（如量子AI导致身份危机）

→ 不能成立 = ✅ 耦合成功，设定是骨
  - 例：因为有了量子纠缠AI，零才能同时存在于多个地点，这是冲突的核心
```

**检查方法**：

```markdown
## 设定必要性检查表

**核心设定**：<填写核心设定，如"量子情感模拟AI">

**去掉设定后的故事**：

- 主线还能成立吗？ □能 □不能
- 冲突还能成立吗？ □能 □不能
- 结局还能成立吗？ □能 □不能

**如果以上任一个"能" → 耦合失败，必须重新设计**

**修正方向**：

- 让设定直接制造冲突（不只是背景）
- 让设定的后果不可逆（角色无法逃避）
- 让结局必须由设定解决（不是靠"爱"或"勇气"就能解决）
```

---

#### 步骤3.6：类型-specific 耦合检查

不同小说类型，检查重点不同：

```markdown
## 类型-specific 耦合检查表

### 科幻

- [ ] 科学设定是否驱动了冲突？（不是背景板）
- [ ] 如果去掉科学设定，故事还能成立吗？（不能 = ✅）
- [ ] 设定的后果是否不可逆？
- [ ] 结局是否必须由科学设定解决？

### 奇幻/玄幻

- [ ] 力量体系是否制造了不可替代的困境？
- [ ] 如果去掉力量体系，故事还能成立吗？（不能 = ✅）
- [ ] 主角的成长是否受力量体系约束？

### 悬疑

- [ ] 谜题是否公平？（线索是否在前文埋好）
- [ ] 真相是否必须由设定推导出来？（不能靠巧合）
- [ ] 凶手的动机是否与设定耦合？

### 爱情

- [ ] 两个人的性格缺陷是否必然导致这个感情走向？
- [ ] 如果换一个性格，故事还能成立吗？（不能 = ✅）
- [ ] 冲突是否来自性格，而不是外力？

### 历史

- [ ] 时代背景是否真正约束了角色的选择？
- [ ] 如果换一个时代，故事还能成立吗？（不能 = ✅）
- [ ] 角色的行为是否符合历史逻辑？

### 都市

- [ ] 职业/社会环境是否影响了情节走向？
- [ ] 如果换一个职业，故事还能成立吗？（不能 = ✅）
- [ ] 冲突是否来自社会现实？

### 其他类型（自定义）

- [ ] <根据类型特点填写>
- [ ] <根据类型特点填写>
```

---

#### 步骤3.7：逻辑自洽性检查

```markdown
## 逻辑自洽性检查表

**设定一致性**：

- [ ] 核心设定的规则，在全文中保持一致（不矛盾）
- [ ] 如果设定有模糊地带，明确定义边界

**设定后果一致性**：

- [ ] 设定对社会/角色的影响，前后保持一致
- [ ] 角色的动机和行为，符合设定约束

**情节因果链检查**：

- [ ] 每个关键情节，都有明确的"因为设定所以..."的理由
- [ ] 没有"机械降神"（突然出现的设定解决所有问题）
- [ ] 没有"精确到不合理"的巧合（概率<5%的事件不能靠巧合）
```

---

#### 步骤3.8：迭代深化（必须执行，至少2轮）

**为什么需要迭代？**

人类作家不会一次就想清楚。他们会反复问自己："这个设定够有趣吗？""这个冲突够强吗？"

AI必须模拟这个过程。

```markdown
## 迭代深化记录

**第1轮**：写出初始设定
  ↓
  **检查**：必要性检查 + 类型-specific 检查 + 逻辑自洽性检查
  **结果**：□ pass □ fail
  **fail 项**：<列出>
  ↓
**第2轮**：针对 fail 项修改设定
  ↓
  **检查**：重新做全部检查
  **结果**：□ pass □ fail
  **fail 项**：<列出>
  ↓
**第N轮**：直到全部检查 pass
  ↓
**最终输出**：深化后的设定 + 耦合检查报告
```

**迭代次数要求**：

- 至少2轮（不达标不能进入下一阶段）
- 如果3轮还不达标，暂停并重新审视核心设定

---

#### 步骤3.9：输出耦合检查报告

```markdown
# 设定-情节耦合检查报告

**项目名称**：<填写>
**检查日期**：<填写>
**迭代次数**：<填写，如"3轮">

---

## 检查结果

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 设定必要性 | ✅ pass / ❌ fail | <说明> |
| 类型-specific 检查 | ✅ pass / ❌ fail | <说明> |
| 逻辑自洽性 | ✅ pass / ❌ fail | <说明> |

---

## 修正记录

| 轮次 | 修正内容 | 修正原因 |
|--------|---------|---------|
| 第1轮 | <如"加强量子AI的不可逆后果"> | <如"初始设定耦合失败"> |
| 第2轮 | <如"让结局必须由量子物理解决"> | <如"第1轮修正后仍不达标"> |

---

## 最终设定（深化后）

<填写深化后的核心设定，简洁版>

---

## 耦合证明

**为什么这个设定值得被写进故事？**

<2-3句话说明，如"因为量子情感模拟AI的设定，主角必须面对'我的情感是真的吗'这个问题，这是故事的核心冲突">

---

**✅ 检查通过，可以进入阶段二（章节计划）**
```

---

**强制规则**：

1. 本阶段**不通过**，不能进入阶段二（章节计划）
2. 迭代深化**至少2轮**，不达标必须修正
3. 耦合检查报告**必须输出**，作为项目文件保存

---

### 阶段二：章节计划（正文创作前必须完成）

#### 步骤5：创建章节详细计划

**强制规则**：在写正文之前，必须为每章生成详细的创作计划。

**存储**：计划创建后**落图**（图库模式：写入图库 `plan_chXXX` 节点；文件模式：保存为 `chapters/plans/ch-XXX-plan.md`）。字段要求见图库模式/文件模式对应清单，缺项=计划不完整（5.1 拒绝创作）。

**为什么需要章节计划？**

AI生成的正文往往"平淡"、"匀速"、"没有重点"，因为AI不知道本章应该"怎么写"。章节计划通过**三要素**给AI明确的约束：

**计划三要素（缺一不可）**：

```markdown
# 第X章 创作计划

## 基本信息
- 章节标题：
- 所属卷：／所属段：
- **段内定位**（必填，2026-08-26 升级）：本章是所属段弧线的节拍——**段 plan 先选节奏体系（救猫咪/故事圈/三幕/七点/弗莱塔格/英雄之旅，见「★ 节奏注入位」），本章标注体系内节拍名**（如 段=救猫咪缩微，本章=催化剂/游戏时间/一无所有…）。位置属性继承旧规则：开头章=触发事件不强行收束；中段章=推进+留白；收尾章=负责弧线闭环
- **完整性属性**：structure = [complete / partial / open]（open=无闭环钩子合法；不完整≠问题，闭环责任在段）
- **节划分**（=分段梗概段落数，一次写一节）：本章共 ___ 节，每节 ≈___ 字
- 预计字数：

---

## ★ 三要素约束（创作时必须严格遵守）

### 要素①：行文风格
控制本章的节奏和句式分布。

**平台调性标注**（必填，来自「目标平台/读者调性」章节）：本章按 [直给型 / 铺陈型] 写。直给型=对话≥50%、描写≤15%、每段推进、无氛围段；铺陈型=允许文学节奏。

**节奏分布**（2026-08-26 删除固定模板——旧 4 选 1（快慢快/慢快慢/匀速/舒缓）与新「节奏注入位」微观规则冲突且更僵化）：
- 章内节奏 = **新微观规则**（见「★ 节奏注入位」→ pacing_micro）：情绪曲线（单章至少一次起伏，不限段数）+ 段落长短交替 + 高潮间隔——按规则自然起伏，不套固定形状
- 段级节奏 = **新宏观体系**（见「★ 节奏注入位」→ pacing 6 体系）：段 plan 选骨架 + 本章节拍名
- 保留的句法层控制（与节奏正交，不属于节奏层）：平台调性（下）/句式混合（下）/章节开头结尾（下）

**句式混合要求**：
- 单句段落：至少 ___ 处（建议：动作场面≥5处，对话场面≥3处）
- 长段落（>200字）：不超过 ___ 处（直给型 ≤1 处）
- 对话占比：约 ___ %（直给型 ≥50%；铺陈型可 30%）

**章节开头方式**（必须与前章不同）：
- 对话切入 / 动作切入 / 感官细节 / 内心独白 / 场景描写
- 检查前章开头方式，确保不重复

**章节结尾方式**（禁止总结性点题）：
- 悬念留白 / 情感余韵 / 行动决定 / 对话截断
- 禁止：在章节末尾添加"这一刻，他明白了..."之类的总结句

### 要素②：创作重点
控制本章的内容比重和偏向。

**本章核心偏向**（单选主倾向，可配次要倾向）：
- [ ] 感情戏（角色关系推进，重对话和内心戏）
- [ ] 剧情推进（事件发生/转折，重动作和因果）
- [ ] 世界背景介绍（设定披露，重环境和规则描写）
- [ ] 环境描写（氛围营造，重感官细节）
- [ ] 动作/战斗场面（重节奏和动词）
- [ ] 内心戏（角色成长/反思，重情绪和独白）
- [ ] 对话戏（信息交换/性格展现，重声线差异）

**重点说明**（2-3句话描述本章最想达成的效果）：
<填写>

**内容比重建议**：
- 根据创作重点，明确各部分字数占比
- 例：对话40% + 动作30% + 内心戏20% + 环境10%
- **⚠️ 必含「主线 vs 填充」占比（#115）**：主线（推进当章目标/长期目标/暗线/核心事件）≥60-70%，填充（插科打诨/氛围/过渡/配角戏）≤30-40%。写正文前先列主线剧情骨架（每段"发生什么+推进什么"）。**补字数=补主线缺口（新信息/新对话/新冲突），禁补填充凑数；补完自测主线占比仍≥60%。**

### 要素③：情绪基调（人感核心）
控制本章的用词选择，让文字携带情绪。

**本章整体情绪**：
- 主情绪：[压抑/亢奋/悲伤/紧张/轻松/绝望/温暖/...]
- 情绪变化曲线：起始情绪 → 转折事件 → 结尾情绪

**角色情绪状态**（影响用词选择）：
- 主角：<情绪状态> → 参照 `characters/<主角>-emotion.md` 的对应词库
- 配角A：<情绪状态> → 同理
- 配角B：<情绪状态> → 同理

**用词约束**：
- 本章所有叙述和对话必须符合上列情绪状态
- 禁止情绪与用词背离（如角色愤怒但用词平和、犹豫）
- 叙述者的语气也必须与章节整体情绪协调

---

## ★ 引用素材位（每章必填 2-4 条，创作时至少自然用 1 处旁征博引）

**目的**：旁征博引=角色引用俗语/名言/歌词/典故/行业黑话来佐证自己（人感核心，让角色"有来历"）；热梗=网络流行语/弹幕（玩家或现代角色）。**不靠"搜索方向"**——写正文时模型没有现成素材就不会引用（error-case #123：搜索方向≠素材，生成时不会凭空调用）。**必须给可直接用的句子**。

**素材1**：<可直接引用的句子/梗>
- 引用者：<哪个角色（须与角色档案的引用素材库匹配）>
- 使用场景：<哪一段/什么情绪/触发条件>
- 引用方式：直接引 / 化用 / 引错（半吊子引用=人设笑点，如文盲装文化人）

**素材2**：<...>
**素材3**：<...>
**素材4**：<...>

**原则**：
- 必须符合角色身份（古代角色不用现代梗；没读过书的不掉书袋）
- 必须自然（不能为引用而引用，宁缺毋滥）
- **频率=节级档位**（2026-08-26 用户拍板：从章维度降到节维度——节=场景/氛围最小单位，章级混合场景无法定档）：写 plan 分段梗概时给每节标引用档（见下）
- 引用者适配：话痨/自认有文化/老江湖类角色=高频引用源；惜字如金类=几乎不引（人设即差异）
- 引用素材优先从角色档案的「引用素材库」选，其次现场造（造完补进角色素材库累积）
- 素材库是开放累积的：踩到好用的一句，补进对应角色的素材库节点
- **⚠️ 机制铁律（2026-08-25 修复，error-case #123 补强）**：素材库必须在正文生成前**随上下文抽取进入写作视野**（写章上下文抽取器须按 type=quote_library 全量拉取，不依赖出边）。否则 plan 素材位会退化为「唯一引用来源」——正文只用 plan 里的 2-4 条例子，库里 10-20 条素材从不被使用，旁征博引机制名存实亡。**plan 素材位=保底清单（至少这些），素材库=可用池（按场景现选，不限于清单）**。

---

## ★ 用词注入位（每章必填，按字数卡频率——正向词汇供给，治「语体单极化/词汇贫乏」）

> **为什么存在**（2026-08-25 用户洞察）：模型语体单极化——朴实则全程朴实、华丽则全程华丽；大量中文词语（尤其歧义大的）在训练时被**概率稀释**（多义项分摊条件概率，任何语境都不如专一词有竞争力），等价于被边缘化。**负向禁词表只是让用词从 top0 切到 top1/top2，时间长了是另一种 AI 味——要给正确方向的指引，不能只禁错误方向。**
>
> **正解**：写 plan 时用**词图查询器**（`references/word-graph/word_query.py`，词库=axolotl 图数据库，同义词词林扩展版 45k 词入库）按**情绪/场景**检索候选词，把符合的人类常用词**注入 plan 上下文**——attention 拉高这些词的生成概率，模型「被提示」用它们（照抄 chunk 是模型强项，创造 chunk 是弱项）。

**流程**：
1. **写 plan 时**跑（图查询，直接查图库，无映射表）：`python3 references/word-graph/word_query.py --emotion 温馨,满足 --scene 种田务农 --person 外形,神态 --action 手部动作 --env 天气 --season 节气 --obj 农具,家什,食物 --sense 声音,触感 --n 40`
   - **8 维度锚点**（2026-08-26 全维度盘点后扩展，43 个子类）：`--emotion` 情绪（14）/ `--scene` 场景（9）/ `--person` 人物描写（外形/神态/肤质）/ `--action` 动作行为（手部/脚步/姿态/表情）/ `--env` 环境景致（天气/光影/山水草木）/ `--season` 时令节气（节气/时节/时辰）/ `--obj` 器物衣食（农具/家什/食物/衣物）/ `--sense` 感官声音（声音/气味/触感）
   - `--extra`=本章自定义词种子（人物专名/书特定词），喂 3 级沾边池
   - 查询路径=纯图：情绪/场景节点 -seed_of→ 种子词 -belongs_to→ 语义类 ←belongs_to- 候选词；命中数=交集加权
   - **三级词池**（2026-08-26 用户拍板，修复「词池单一→该换的词换不进去/情绪词堆砌」）：
     - **1 级·人感**（**情绪∪感官 并集**——情绪词+感官词都是人感词，同一体验两面视角；同时命中两面者排最前。2026-08-26 用户拍板：先交集后改并集——交集会丢词）：**人感主要来源，最高级**（暖/舒心/绵软/叹气/发麻 都在此级）
     - **2 级·词汇**（其余维度：场景×器物、单维度高频等）：**消除单调用词**，非人感——补充，按需用
     - **3 级·沾边**（`--extra` 种子扩展：具体名词/人物词/本章关键词）：大池备用——具体名词、动作词、场景词都在这里（如 垄/田埂/耘锄/阡陌）
   - **用法比例**：1 级人感词=正文灵魂（高频）；2 级词汇=消除单调（中频）；3 级=按需补充
   - 兜底备选（文件版）：`references/word-injector/word_injector.py`（词林 txt + 种子映射，无图库环境时用）
2. 三级都看，**比例上 3 级占大头、1/2 级情绪词只作点缀**（教训：女频 ch1 全用 1 级情绪词→句尾堆砌「踏实，实在，舒心」）；人工挑最贴场景的填进本字段
3. 正文**按字数比例自然使用**：**每约 20 字 1 个**（2026-08-26 用户拍板：100 字/个仍偏低；2800 字章≈140 个，3500 字≈175 个）——**不是点缀而是主流**：全文词汇由注入词驱动，几乎每句都从清单选词（漂移词自然忽略，只选贴合的）
4. **selfcheck 校验**：本章注入词清单 → 实际出现 ≥ 字数/20 个；不足=补

**用词注入位**：
- 本章情绪/场景：<如 温馨/满足 + 种田务农>
- 注入词清单：<100-150 个候选（字数/20 的量级），三级合并：3 级具体名词为主 + 1/2 级情绪词点缀，如 垄/田埂/耘锄/草木灰/畦 + 暖/踏实/舒心…>

**与禁词表的关系**：AI 痕迹词库（显然/非常那类）**降级**为次要手段（不再作主力，只兜底）；文言硬错词（尔/汝/岗顶）**照禁**——那是错误不是风格，正向注入与硬错排除不冲突。图库已内置文言缩词 ban 属性（复用 baihua-lexicon，查询时自动排除）。

**词图说明**（`references/word-graph/`）：
- `lexicon.axeb`：图库真源（词 45k / 语义类 9,988 / belongs_to 边 55k；8 维度 43 锚点（情绪14/场景9/人物3/动作4/环境3/时令3/器物4/感官3）；句式范式 30）。重建：`python3 import_cilin.py`（词林 txt + seed-map 导入）+ `python3 import_syntax.py`（句式范式导入）
- **可累积**：踩到好词往情绪/场景锚点补 seed_of 边，或往 `word-injector/seed-map.json` 补种子后重导；句式范式往 `syntax-patterns.json` 加后重导

---

## ★ 句式注入位（每章必填，治「句式单极化」——用词注入的同构方案）

> **为什么存在**（2026-08-25 用户提出「同样的方案是不是也能处理句式」）：模型不只词汇贫乏，**句式同样单极化**——总是主谓宾直叙 + 「X说」标签 + 短句堆叠，整章一种骨架。句式是结构层的「词」：可枚举（黄廖《现代汉语》句型/句式/句类/复句四套权威分类）、可注入、few-shot 模仿是模型强项。**给范式+例句，模型照抄结构；不给，它就只会自己的那几套。**

**数据源**（权威转录，非发明）：黄廖《现代汉语》四套分类体系 → `references/word-graph/syntax-patterns.json`（30 个范式）→ 已入图库（pattern_{名} -fits-> emotion_/scene_ 锚点，复用词图情绪/场景）。

**流程**：
1. **写 plan 时**跑：`python3 references/word-graph/syntax_query.py --emotion 无奈 --scene 谈判要价`（或 `--scene 夜袭战斗`/`--all` 看全表）
2. 把**最贴合本章场景的范式**（带例句）填进本字段——**不设数量要求，按需选最合适的**（2026-08-26 用户拍板：句式本就不多，每句找最合适的用才对，「全章轮换 N 种」是无谓的达标约束）
3. 正文**每句按需选最合适的句式**；约束只有一条：**每个自然段最多 1 种特殊句式**（用户拍板，防堆砌/防句式打架）——句式自然多样化，不刻意凑轮换
4. **selfcheck 校验**：只查「整章句式是否单一」（自查相邻段首重复/「X说」标签密度）——单一才补，不设数量下限

**句式注入位**：
- 本章句式清单：<贴合场景的范式，如 转折复句（规矩是铁的，可架不住磨）/条件复句（只要你报备，我就批）…——按需选，不为凑数>

**与用词注入位的关系**：词管「用什么词」，句式管「用什么骨架」——两层互补，写 plan 时一起填；词按频率落地（每约 20 字 1 个），句式按需落地（每段最多 1 种，无数量目标）。

---

## ★ 技法注入位（每章按需填——修辞/物象/转场/声口，正向注入的第四层）

> **为什么存在**（2026-08-26 用户：「除了词语和句式以外，是不是还有其他的方面也可以这样处理？比如修辞手法」——正向注入是通用机制：凡「可枚举素材 + 模型用不好」的层面都能套）。词管血肉、句式管骨架、引用管人味、**技法管手法**——修辞治比喻套路化、物象治描写空泛、转场治过渡生硬、声口治角色一个调。

**四库**（`references/word-graph/`，**图库为主**（2026-08-26 用户拍板：技法库也主推图库，无图库时文件兜底）；查询 `python3 craft_query.py --rhetoric/--imagery/--transition/--voice`，`--file` 强制文件版）：
1. **修辞**（rhetoric.json→图库 rhetoric_{名}，陈望道《修辞学发凡》转录 12 格）：比喻/拟人/夸张/通感/排比/借代/反复/反问/设问/对比/双关/反语——每格=结构+去套路化范例+频率（**节级档位**，见「引用/修辞节级档位表」）；-fits-> 情绪/场景锚点可过滤
2. **物象**（imagery.json→图库 imagery_{场景}，14 类场景/情绪→具体物象）：写『冷』不写冷，写 霜花/白气/冻裂的缸沿——治描写空泛
3. **转场**（transition.json→图库 transition_{名}，8 种）：对话/物件/声音/时间/省略/动作/事件/情绪转场——替代「突然/就在这时」
4. **声口**（voice.json→图库 voice 节点）：通用语气词（应答/惊讶/无奈/催促/抱怨/得意）+ 设计方法（标志性口头禅+语气词区间+句式习惯）
   重建：`python3 import_craft.py`（rhetoric/imagery/transition/voice.json → 图库）

**流程**：
1. 写 plan 时 `python3 references/word-graph/craft_query.py --rhetoric --imagery 冷,穷 --transition --voice`
2. 按本章场景/情绪挑：修辞 2-3 格、物象 2-3 个、转场 2-3 种、声口（出场角色定口头禅）填进本字段
3. 正文落地：**按触发位置用技法**（转场在切换点/物象在描写处/声口在台词/修辞格在情感氛围幽默点，见「技法触发位置表」）——密度是结果不是目标
4. **selfcheck 校验**：逐节非白描句子占比≈50%+；场景切换无「突然/就在这时」；角色声口有区分度

---

## ★ 引用/技法触发位置表（2026-08-26 用户拍板：按需触发，替代章级固定数量与百分比分配）

> **为什么**：引用频率由「话痨浓度」（角色声口）、技法构成由「场景类型」决定。**白描=修辞的一种（消极修辞）**——修辞/物象/转场/声口是**人类写作的正常构成**（不是高级点缀），人写文章各种技法平均出现；当前 AI 文 80-90% 纯白描（消极修辞）+偶尔修辞格=不人感。节=场景/氛围最小单位（≈500-900 字），场景类型单一，构成比例精确；章节合计=各节自然加总。

**技法触发位置表（2026-08-26 用户拍板：按需触发，替代百分比分配——百分比是后验统计不是写作指导）**：

| 技法 | 触发位置（什么时候用） | 注意 |
|---|---|---|
| **转场** | **节与节之间/场景切换点必用**（8 种转场手法选一，禁硬切） | 动作场景同样要转场（声音/动作/事件转场照常） |
| **物象** | **一切具体描写处**（场景/物件/动作/感官）——写『冷』写霜花/白气，写『穷』写米缸见底 | 禁抽象空泛（风很大/天很冷） |
| **声口** | **一切角色台词**（口头禅/语气词/句式习惯） | 角色区分度；禁所有角色共用一套语气词 |
| **修辞格**（积极修辞） | **情感点/氛围点/幽默点/强调处**（按需调味） | 白描是常态底座；动作戏禁比喻（减速带） |
| **引用** | **话痨角色在装文化人/讲道理/吹牛/被质疑处**（角色声口决定密度） | 紧张/逃命场景不掉书袋 |

**核心原则**：
1. **白描=修辞的一种**（消极修辞，常态底座）；修辞格=积极修辞（升级调味）——不是对立，是修辞内部两档
2. **「在需要的地方用对应的写法」**：转场在切换点、物象在描写处、声口在台词、修辞格在情感/氛围/幽默点——所有触发点满足=自然人感，无需人为设定百分比
3. 技法密度是**结果不是目标**：触发点都满足后，修辞格+物象+转场+声口密度自然≈50-60%（远高于当前 AI 文 80-90% 纯白描）

**selfcheck 按触发点校验**：逐节查——场景切换点是否用了转场（禁硬切）；描写处是否有物象（禁空泛）；角色台词是否有声口（区分度）；情感/氛围/幽默点是否有修辞格（白描底座上）；不符=该节补对应技法（不是删）。**selfcheck 按节校验**：逐节查该节非白描句子占比≈50%+、引用密度符合场景类型；不符=该节补技法（不是删）。

## ★ 节奏注入位（卷/段级选骨架 + 章节级节奏规则——叙事节奏，第五层）

> **为什么存在**（2026-08-26 用户：「叙事节奏我觉得也应该有人总结过」→ 搜到权威体系，建多体系节奏库）。节奏=故事的呼吸：宏观骨架（卷/段）选一个权威体系，微观（章节）按规则卡。

**节奏库**（`references/word-graph/pacing.json` → 图库 pacing_{体系}，查询 `python3 craft_query.py --pacing`）：
- **宏观 6 体系**（story/segment 级骨架，落图时选一个）：
  1. 救猫咪 15 节拍（Blake Snyder，带百分比，最实用）
  2. 故事圈 8 步（Dan Harmon，**可递归到单章/单场景**，喜剧/连续剧利器）
  3. 三幕结构（Syd Field，25/50/25，诊断中段疲软）
  4. 七点结构（Dan Wells，钩子→转折→夹点→中点→夹点→转折→结局）
  5. 弗莱塔格金字塔（铺垫→上升→高潮→下降→结局，古典）
  6. 英雄之旅 17 阶段（Campbell，长卷神话级）
- **微观规则**（章节级，pacing_micro）：章节钩子/情绪曲线/笑点密度/段落长短交替/信息释放/场景切换/高潮间隔
- 特鲁比 22 步细节不确定未收录（防幻觉，后续补）

**流程**：
1. **卷/段落图时**：`python3 references/word-graph/craft_query.py --pacing` → 选一个体系做骨架，段 plan 的【分段梗概】对照体系节拍落位（如段=救猫咪缩微：铺垫→催化剂→游戏时间→中点→一无所有→终场）
2. **写章节 plan 时**：`--pacing micro` 对照微观规则——钩子/情绪曲线/笑点密度/段落节奏逐条过
3. **正文落地**：宏观=段弧线方向（写每节时知道自己在弧线的哪个节拍）；微观=章节内节奏执行
4. **selfcheck 校验**：段末查弧线是否按体系节拍闭合；章末查微观规则（钩子有/情绪有起伏/段落长短交替）

**与层级完整性（阶段零·五）的关系**：节奏体系=骨架的形状，层级=骨架的挂载（卷→段→章→节）——段 plan 落图时既挂 contains 边（层级）又标节拍名（节奏）。

---

## ★ 叙事手法注入位（第六层——对话/开篇/战斗/悬念/场景/喜剧）

> **为什么存在**（2026-08-26 用户：「还有我们漏掉的方向吗」→ 盘点：语言层+节奏层已覆盖，**叙事手法层整层空白**——管「场景怎么搭、对话怎么打、反转怎么翻」）。模型在这层的 AI 味：对话呆板/开场平/动作糊/伏笔不收。

**六库**（`references/word-graph/`，图库 narrative_{库}_{手法}，查询 `python3 craft_query.py --dialogue/--opening/--action/--suspense/--scene/--comedy`）：
1. **对话技术**（dialogue.json，9 条）：潜台词/冲突对话/短促交锋/答非所问/打断抢话/对话推信息/复述确认/留白沉默/移花接木——治「一问一答呆板」
2. **开篇/黄金三章**（opening.json，8 条）：冲突开场/悬念钩子/快立人物/信息控制/反常开局/目标前置/贵人引子/三章验收——治「开局平留不住人」
3. **战斗动作**（action.json，9 条）：动作分解/短句节奏/力量感/过程与结果/攻防往返/受伤代价/以弱胜强/旁观视角/战斗收尾——治「招式糊节奏平」
4. **悬念反转**（suspense.json，9 条）：伏笔埋设/回收/误导/反转铺垫/信息顺序/悬念密度/升级悬念/揭晓时机/余味——治「伏笔不收反转没铺垫」（与项目伏笔表配合）
5. **场景构建**（scene.json，8 条，麦基《故事》）：场景目标/价值转变/入场/退场/转折/节奏/串联/省略——治「场景没功能」（**核心：一个场景=一个价值转变，没转变=删**）
6. **喜剧手法**（comedy.json，10 条）：误会/错位/一本正经/重复升级/意外加合理/立flag打脸/自嘲/夸张反应/冷幽默/反讽——治「有梗但手法单一」（与整活梗库配合）

**流程**：
1. 写 plan 时按本章场景查对应库（`--dialogue` 对话多的章/`--action` 战斗章/`--comedy` 喜剧章…）
2. 挑 2-3 条手法填进本字段（如 本章用 潜台词+答非所问+立flag打脸）
3. 正文落地：手法是「怎么演」，配合词/句式/技法层「用什么演」
4. **selfcheck 校验**：本章场景有价值转变（麦基）；对话无「一问一答呆板」（自查）；伏笔埋了有回收计划（对伏笔表）

**与各层的关系**（七层全景）：
```
词（血肉）→ 句式（骨架）→ 引用（人味）→ 技法（手法：修辞/物象/转场/声口）
→ 节奏（呼吸：宏观体系+微观规则）→ 叙事手法（招式：对话/开篇/战斗/悬念/场景/喜剧）
→ 负面约束（护栏：禁词/破折号/堆砌）
```
语言层（词/句式/修辞）管「说得好不好」，叙事手法层管「戏演得对不对」——正交互补。

**新旧节奏设计边界（2026-08-26 清理，防冲突）**：
- ❌ **删除**：旧「节奏分布 4 选 1」（快慢快/慢快慢/匀速紧张/舒缓铺陈）——章内固定三段式模板，与新微观规则（情绪曲线自由起伏）重叠且僵化；「匀速紧张」与负面约束「禁节奏均匀」字面冲突
- 🔧 **升级**：旧「段内定位」→ 段选体系 + 本章节拍名（宏观层归新机制）
- ✅ **保留（句法/平台层，与节奏正交）**：平台调性（直给/铺陈）、句式混合（单句段/长段/对话占比）、章节开头/结尾方式——这些管「句法细节」，节奏层管「起伏形状」，边界清晰不冲突
- **一句话边界**：节奏层=「什么时候紧、什么时候松」（宏观体系+微观规则）；句法层=「一句话多长、用什么句式」（平台调性/句式混合）——两层各自管，plan 里分开填

---

## ★ 类型卖点清单（Genre Payoff Checklist，按书核类型必填）

> **为什么必填**：plan 三要素（行文风格/创作重点/情绪）只约束"怎么写"，**不约束"该类型读者的核心期待怎么兑现"**。搞笑书写出来不好笑、爽文不够爽、悬疑没钩子、感情没推进——根因都是**写之前没把该类型的"卖点清单"输入 plan**（error-case #107/#108）。喜剧缺的是笑点清单，爽文缺爽点清单，悬疑缺线索清单，感情流缺关系节点……**每种类型都有对应的"卖点清单"，按书核确定的类型选填，写完按清单自测，一次到位。**

**使用**：书核确定类型（可复合，如"搞笑+悬疑"=笑点+线索双清单）→ 每章 plan 必填对应清单 → 写完按清单逐项自测（数够不够、强度够不够），不过即补，不等用户反馈。

### 笑点清单（搞笑/喜剧卖点，7-10 个/章，每 300-500 字一个）

| # | 触发场景 | 笑点机制 | 升级层次 |
|---|---------|---------|---------|
| 1 | <哪一幕> | <荒诞升级/反差/错位/自嗨夸张/弹幕/黑色幽默> | <反应/自嗨夸张到哪一层> |
| 2 | ... | ... | ... |

- **整活位**：本章爆笑点（精神污染/大场面/反转）
- **弹幕位**：玩家/观众视角吐槽插入点（游戏元叙事类）
- **自嗨夸张位**：主角自我感觉夸张到欠揍的段落
- 机制示例：荒诞升级=反应层层离谱（耳塞→灯灭→瓦片掉）；反差=自嗨陶醉 vs 台下空一半；错位=嘴上 A 实际 B；弹幕=读者视角吐槽

### 爽点清单（爽文/打脸流，每章至少 1 个完整爽点闭环）
- 闭环：装逼→铺垫→打脸（反派原形毕露/主角反杀/身份揭露/升级突破/捡漏收获）
- 爽点用动作和对话承载，作者不掰碎解释（#045）；主角不吃瘪超过预设章节数

### 线索清单（悬疑/推理，每案必填）
- 本章埋什么线索（显性/隐性）、误导（红鲱鱼）、反转节点
- 伏笔回收表：章 X 埋 → 章 Y 收（不得遗漏）；公平性=读者能靠前文线索推出真相

### 关系节点清单（感情流/言情，每章至少 1 个情感推进点）
- 节点类型：心动/误会/试探/亲密/危机/和解/告白
- 禁无进展的重复拉扯；情感推进用对话或动作承载，不靠旁白总结

### 收获节点清单（升级/成长流，每章至少 1 个推进）
- 收获类型：突破/领悟/得宝/升阶/人脉/情报
- 力量体系进度表：现在 → 目标（卷内节点），主角成长受体系约束（类型-specific 耦合检查）

### 惊吓节奏清单（恐怖/惊悚，每章 2-3 个惊吓点）
- 惊吓点分布：铺垫 → 爆发 → 余悸；氛围曲线有松紧，不全程高压

### 博弈节点清单（职场/现实/宫斗，每章 1-2 个博弈点）
- 节点：冲突/试探/反杀/交易；输赢都有后果，不空转

---

## ★ 强制用词清单（从词库中选择，必须执行）

**本章必须使用的情绪用词（从情绪词库中选择）**：

| 角色 | 情绪状态 | 必须使用的动词（至少选2个） | 必须使用的形容词（至少选1个） | 句式要求 |
|------|---------|--------------------------|--------------------------|---------|
| 主角（零） | <情绪状态> | <从词库选择> | <从词库选择> | <从词库选择> |
| 配角A | <情绪状态> | <从词库选择> | <从词库选择> | <从词库选择> |
| 配角B | <情绪状态> | <从词库选择> | <从词库选择> | <从词库选择> |

**本章必须使用的对话标签（从对话标签库中选择）**：

| 场景 | 角色情绪 | 必须使用的标签（禁止用"说/道"） |
|------|---------|------------------------------|
| 场景1 | <情绪> | <从标签库选择> |
| 场景2 | <情绪> | <从标签库选择> |
| ... | ... | ... |

**本章必须模糊化的数字（从模糊数字词库中选择）**：

| 原文数字 | 模糊表达（从词库选择） |
|---------|---------------------|
| <如有> | <从词库选择> |
| ... | ... |

**本章禁止使用的词（AI痕迹特征词）**：

- [ ] 非常、十分、极其、相当（AI常用空词）
- [ ] 说、道（必须改用情绪标签）
- [ ] 前3章高频词：<列表>
- [ ] 其他：<根据需要添加>

---

## ★ 负面约束（禁止事项，创作时必须逐条检查）

> 本清单由 error-cases 库「生成前置」类规则提炼（每条标注来源 #号），**动笔前逐条过，写作中对应避免**。机检类（#020/#023/#024 等）由 check_error_cases.py 兜底，不重复列入。

- [ ] **禁止直白点题**：主题必须通过情节/动作/对话自然体现，不得在文中出现"这说明..."、"他意识到..."之类的直白总结（#017/#046/#068/#073）
- [ ] **禁止精确数字**：除严谨必要场景（如科技设定、战斗倒计时）外，所有时间/距离/数量必须用模糊表达：
  - ✅ 片刻、数十米、百来步、半刻钟、三两下、片刻之后
  - ❌ 37秒、142步、8.5米、23分钟、正好3次
  - 例外场景每章不超过3处，且必须在计划中注明
- [ ] **禁止重复前章用词**：从前面3章提取高频形容词和动词，本章强制避开（#059）
- [ ] **禁止连续3段使用相同句式**：每段结束后检查下一段的句式是否变化（#066：段落开头连续以代词「他/我」起头亦禁）
- [ ] **禁止节奏均匀分布**：段落长度必须有意混合短段（1-2句）和长段（5句以上），不得全部是3-4句的中等段落
- [ ] **禁止使用"略过/跳过/很快/不久之后"等水字数过渡词**
- [ ] **禁止句尾堆砌同义情绪词**（2026-08-26 女频 ch1 教训）：注入词不得在句尾挂「踏实，实在，舒心」「喜滋滋的，乐呵呵，欢喜」式同义尾巴——同义堆叠=复读机。注入词要**嵌进句子结构**（作定语/状语/动词/具体描写），情绪由叙述承载，不靠句尾贴词（与 #125「情绪进措辞不进身体」同源）
- [ ] **禁止破折号（——）非必要使用**（2026-08-26 用户拍板：原「≤3」作废，改**非必要全禁**，机检默认 0）：破折号是 AI 高频痕迹（爱用其做插入语/解释/转折）；仅**必要修辞**可保留——声音拖长（「嗷呜——」）、明确语气转折等，且保留处须在 selfcheck 注明理由；非必要一律改写为逗号/句号/换句式
- [ ] **禁止动作后补内心总结/感慨**：角色做完动作/选择后，删紧随的「这下够了/够用便好/实打实进项/说到底就是X」类感慨句；意思已由动作承载（#017）
- [ ] **禁止作者跳出点题/章首章尾抒情**：章首/章尾/段间，主动搜「这回/今日/终究/说到底/才算/每一刀/摊给世人/便是那该被看见的X/才敢说这句/像在替他开口」等信号句并删；角色台词内的箴言合法，作者旁白复述非法（#073/#068）
- [ ] **禁止否定式冗余描写**：不用「不X不Y」打预防针、模糊拟人比喻、「不是X是Y」澄清冗余（#025）
- [ ] **禁止「X了一分」/「一X」廉价节拍**：套路化突变量词与名词+一+动词节拍（#020/#023，机检兜底，创作时主动避免）
- [ ] **禁止默记线索套路**：不写「刻在心里/暗中记下」式信息处理偷懒句；线索以可见动作承载（#027）
- [ ] **禁止判断后接解释性尾巴**：不写「——X的那种Y」把意思重描一遍；判断/叙述说完即止（#039）
- [ ] **禁止作者跳出来解说主角战术/自夸**：爽感场景让动作说话，作者不掰碎爽点（#045）
- [ ] **禁止情绪身体扫描（AI 平感第一破绽）**：禁「情绪名词+身体部位」式写法——心口沉/肩膀拧紧/脊梁发紧/脊梁像旧门轴/心里那根刺/眼眶发热/喉头一哽 等，把情绪翻译到身体上。合法的是**具体身体事实**（累了捶背、手背裂口、冻得缩脖）与**声口措辞携带情绪**（同一事物用角色视角命名/选动词/定褒贬，见 #125）。情绪必须进措辞，不进身体（#074/#125）
- [ ] **禁止设定化措辞/旁白代体验**：叙述不用「某种力/那线/这印」类设定黑话代体验；人物感受用五感细节（#086）
- [ ] **禁止叙事推进空转**：问答不得同义复读、相邻段不得重复、状态不得跳跃、动机不得偏移（#087）
- [ ] **禁止主角独白立场错位**：写「信我」实际要「疑对手」时，立场写对（#082）
- [ ] **禁止过量内心独白/心理推演**：小说是故事不是「主角怎么想」；长段推演压成一句动作或留白（#014）
- [ ] **内心戏须自言自语式，禁旁白式总结**：角色内心活动写成说出口的嘀咕/喃喃（带引号），禁「他想/他明白/他记在心里」+概括句的旁白转述；短判断（耳朵一竖/心里咯噔）可留旁白，超过一句的内心活动必须开口；不得复述读者刚看到的剧情（#112）
- [ ] **叙述语言口语化，禁突然文绉绉**：通俗调性书=旁白与对话共用一套口语基准（大白话短句），禁旁白文雅/对话口语两层皮；世界观功能词（批文/核销/评级）≠交流用词，角色跟玩家/现代语境角色说话时换大白话（审批/走流程/好处）；「在下/久仰/帮衬/劳烦」类江湖腔直接删（#113）
- [ ] **禁止设定符号过度复读**：核心视觉化符号（某线/某纹）每次出场不重建，只点关键处（#006/#019）
- [ ] **禁止单信息同章重复**：同一认知/同一信息一章内不推演两次、不两处交代（#017/#087）
- [ ] **在场角色勿反复直呼全名**：除首次引入身份外，在场角色用代词（他/这小子/一旁那人）指代；直呼全名=不在场信号（叙述者从远处报名字）。需点名时先「指」后「名」（「这小子便是X」）。不在场角色（已离场/被锁别处）用全名反而正确（#072）

---

## 设定点引用计划

本章可能涉及的设定点（从范例库选取，不得自创点题方式）：

| 设定点 | 选用范例角度 | 改编说明 |
|--------|-------------|---------|
| <设定点名> | 角度X | <如何改编，避免与前次使用重复> |
| ... | ... | ... |

---

## 伏笔计划

- **埋入**：[章X] <事件> → 预期回收章Y
- **收回**：回收前章伏笔 <编号>

---

### 图库 plan 字段清单（图库模式必填）

> **用途**：plan 落图（`plan_chXXX` 节点）时，content 必须按此字段清单写全，缺一即视为计划不完整（5.1 拒绝创作）。本清单是「计划三要素」的图库化落点，写 plan 时逐项填充。

- 【时间线】：起点时段/跨几个时段/与上章衔接点（必填）
- 【调性】：直给型/铺陈型（必填，来自「目标平台/读者调性」）
- 【标题】：章节标题
- 【分段梗概】：本章按叙事顺序切 3-8 段，每段=「情节简介（一句话）+ 篇幅占比（或预计字数）」。**写正文前最高优先级的结构约束**——管「写什么顺序、各占多少」；段序不变、篇幅不擅自扩缩超 ±10%；主线段合计 ≥60-70%（#116，#115 联动）
- 【三要素】：
  - ①行文风格：节奏分布/句式混合/章节开头方式/章节结尾方式（必填）
  - ②创作重点：核心偏向/内容比重（必填；内容比重必含「主线 vs 填充」占比：主线≥60-70%、填充≤30-40%，先列主线剧情骨架；补字数=补主线缺口非填充，#115）
  - ③情绪基调：主情绪/情绪变化曲线/角色情绪状态/用词约束（必填）
- 【声口】：本章各角色语言特征（口癖/自称/句式差异）
- 【对话人物维度】：本章**每个有对话的角色**逐人五要素（时间=此刻时点/与事件间隔；空间=在哪/是否在场；已知=知道什么；未知=不知道什么【重点】；心态=情绪/立场/动机）。学舌/套话/传话类桥段必须写清「目标不在场」+「增量信息」。写正文前对照五要素审一遍对话清单（#114）
- 【禁词】：本章禁用词（AI痕迹特征词 + 前章高频词）
- 【设定语映射】：**本章出现的每个设定专名/机制概念，首次出现须标注「正文口语说法」**（例：新冒险地图第一小镇→正文说「新开的图，头一个镇子」；贡献值→「差一两个任务的事」；规环印→伏笔期只写「没人认得的纹样」）。正文写手直接从映射拿口语，**禁把设定术语直接写进 NPC 台词/旁白**（角色说话=角色视角，不念设定）。写 plan 时逐词过：这词角色会怎么说？
- 【伏笔】：本章埋入/收回的伏笔
- 【钩子】：章末钩子
- 【笑点清单】：类型卖点清单（喜剧=笑点 N 个/章，逐条列出）；**标注哪个是「剧情级大笑点」（★）**——每 2 章至少 1 个，连续 2 章无★=并章信号
- 【整活位】/【自嗨夸张位】/【弹幕位】：可选，表演位/弹幕位标注
- 【衔接】：→ 下一章（目标章存在）
- 【暗线】：本章暗线进展

**校验**：写完 plan 后逐项核对上述字段是否齐全（含【分段梗概】+【对话人物维度】五要素+【设定语映射】）；图库模式可用机检（扫 `plan_chXXX` 节点，缺字段即报错）。**审核 plan 时再审一次**：①【分段梗概】——顺序是否合理（因果链/反转位置）、主线段合计是否 ≥60-70%、每段能否展开成「场面」（#116）；②【对话人物维度】——逐人过对话清单，「某人会说出他未知的信息」或「某段对话时空前提不成立」（目标该在场却不在场/已说过的话又重复）当场改 plan，不等正文出错（#114）；③【设定语映射】——通读 plan 的专名/概念，逐个问「这词角色会怎么说」，没有口语映射的设定语要么补映射、要么改成角色会说的说法，**禁让设定语原样流进正文**（#113 同族：角色认知边界）。

### ⚠️ plan 结构自检（写正文前必过，防结构崩坏）

> **用途**：字段齐全 ≠ 结构可用。字段是「有」，结构是「好」。**案例 P001（某章查底细）教训**：笑点清单 8 个里 6 个是信息获取型（打听/查档/套话/拼线索），照抄进正文=打听消息流水账；用户否结构后 7 轮修正全在错误骨架上打补丁，修改轮数 vs 前章从 1-3 轮断崖到 7 轮。**plan 自检必须在写正文前做**——此时改成本最低（改 plan 几行 vs 重写整章）。

**自检清单（5 问，任一不过则重设计 plan 对应部分）**：
1. **笑点类型审查**：笑点清单里「信息获取型」（问信息/被拒信息/看档案/拼线索/总结/流程）占比是否 >50%？是 → 重设计成「事件互动型」（主角在做事/跟人互动/认知错位/有反转）。判据：每个笑点能否被读者当「场面」复述（如「送酒被报价破防」「同行秀原版压场」）？不能复述成场面=信息型。
2. **锚点类型审查**：本章主事件是「查/摸底/打听/确认」类信息动作吗？是 → 必须补一个「喜剧事件内核」（冲突/设局/表演/被搅动），获取信息只能作为事件的结果，不能是事件本身。
3. **场景推进物检查**：每个场景的推进物是「事件」（对手做了什么/主角做了什么/有反转）还是「信息」（问了什么/知道了什么）？信息推进场景 ≤ 事件推进场景。
4. **跨章钩子类型**：上一章结尾钩子是「任务指令」型（如「明天去找 X 打听」）吗？是 → 本章开场要主动补事件内核，别顺着任务指令走成流程章；写本章结尾钩子时，留给下章「事件/冲突」空间，别写任务指令。
5. **记忆点检查**：本章有没有 1 个「可被读者记住的笑点场面」（重场戏）？plan 里标出哪个笑点是记忆点，正文优先保它。
6. **内容量预算（与首稿字数阈值联动）**：按 2800-3800 字/章粗估，本章笑点/场景够不够撑满？笑点重复（同模式第 2 次出现）、信息获取型过半、场景数 ≤2 且无大场面 → **内容量不足信号**：优先压缩合并章节（相邻章并章），禁止靠硬灌水凑字数。plan 阶段就拦，别等首稿写短了再补。

**对照案例库**：plan 层错误案例见 `references/plan-error-cases.md`（前置库，与后置 `error-cases/` 区分）。

---
```

---

### 阶段三：正文创作（严格执行计划）

#### 步骤6：撰写章节

**创作流程（必须严格执行）**：

**5.1 加载计划**
- **图库模式（推荐/默认）**：从图库拉取本章计划节点 `plan_chXXX`（跑项目工具 `tools/fetch_chapter_context.py <chXXX>`，或直接读取图库该节点 content；见「图库 plan 字段清单」）
- **文件模式（未启用图库）**：读取本章的 `chapters/plans/ch-XXX-plan.md`
- 如计划不存在或三要素不完整，**拒绝创作，先补全计划（补全后落图/落盘）**
- **plan 未过「结构自检」（见上文 5 问）同样拒绝创作**，先重设计对应部分再落图

**5.2 加载设定和范例**
- 读取相关设定文件
- 读取本章涉及的设定点写作范例（从 `settings/examples/`）
- 读取本章涉及角色的 `characters/<角色名>-emotion.md`

**5.3 按三要素创作（强制执行词库查阅）**

**⚠️ 重要：每段创作前，必须执行以下查阅步骤（不可跳过）**

```
【第一段前】查阅词库（必须完成，否则不动笔）

步骤1：查阅情绪词库
  📖 打开：settings/characters/<当前角色名>-emotion.md
  ✅ 必须完成：
     - 确认当前角色的情绪状态（愤怒/自卑/悲伤/...）
     - 从"动词倾向"中选择本段要用的动词（至少1个）
     - 从"形容词倾向"中选择本段要用的形容词（至少1个）
     - 确认句式符合"句式倾向"
  ❌ 禁止：跳过查阅，凭"感觉"写

步骤2：查阅对话标签库
  📖 打开：references/style-guide.md → "对话标签词库"部分
  ✅ 必须完成：
     - 如果本段有对话，必须从情绪标签中选择（禁止用"说/道"）
     - 检查前3次对话用了什么标签，确保不重复
  ❌ 禁止：连续使用相同标签

步骤3：查阅模糊数字词库
  📖 打开：references/style-guide.md → "模糊数字表达词库"部分
  ✅ 必须完成：
     - 如果本段有数字，必须替换为模糊表达
     - 例外场景（科技设定/战斗倒计时）必须在章节计划中注明
  ❌ 禁止：使用精确数字（除非已注明例外）

【整章写完后】正文↔图库双向同步（图库管理设定的项目必做）

> **目的**：保证后续章节引用细节（首饰/独有装备/地点/规矩）时不冲突。动笔前是"下行"（图库→正文），写完必须有"上行"（正文→图库）+ 冲突比对，形成闭环。

**1. 冲突比对**：把正文出现的实体/设定与图库基础设定比对
- 冲突（图库已定但正文写反/写错）→ **修正文**（图库为真源）；若正文新写法明显更合理 → 更新图库并在记忆记录改因
- 手段：跑项目一致性校验器（阶段零步骤C规则）+ 专名/角色名 grep 比对

**2. 新设定回流**：正文写了图库未明确的事项（人物首饰、独有性装备、新地点细节、组织细节、专属规矩等）→ **回流图库**：
- 补挂到已有节点：相关角色/地点/组织节点 content 追加「【正文回流】chXXX：<新设定>」
- 或新建节点（新实体 type=concept/person，边挂相关）
- 回流清单记入 tracker/selfcheck：章号 + 新增项 + 落点（哪个节点）

**3. 判定标准**：写完一章，问"正文里有没有图库不知道的细节？"——有则回流；没有则过。宁可多回流，不可留孤儿设定（正文有、图库无=后续必冲突）。

【每段写完后】自查（必须完成）

要素① 行文风格 → 自问：
  ✓ 这段的节奏对吗？（快/慢是否符合计划）
  ✓ 句式是否变化了？（避免连续相同句式）
  ✓ 段落长度是否混合了？（避免全部是中等长度）

要素② 创作重点 → 自问：
  ✓ 这段是否符合本章的创作重点？
  ✓ 内容比重是否符合计划？

要素③ 情绪基调 → 自问（对照词库）：
  ✓ 我查阅了情绪词库吗？（必须回答"是"）
  ✓ 这个词符合角色当前的情绪状态吗？（从词库中选择）
  ✓ 这个句式符合章节的整体情绪吗？（从词库中选择）
  ✓ 我查阅了对话标签库吗？（如果本段有对话）
  ✓ 我查阅了模糊数字词库吗？（如果本段有数字）
```

**词库查阅证明（必须附加在正文后）**：

每段写完后，在创作说明中注明：
```
> 词库查阅记录：
> - 情绪词库：已查阅（角色：<名字>，情绪：<状态>，选用动词：<X>，形容词：<Y>）
> - 对话标签库：已查阅（本段对话标签：<标签>，前3次标签：<列表>）
> - 模糊数字词库：已查阅（本段数字：<如有>，处理方式：<模糊化/例外>）
> - 引用记录：<如有引用，注明搜索关键词、选用内容、化用方式>
> - 流行文化引用记录：<如有引用，注明搜索关键词、选用热梗/流行语、化用方式>
```

**【修复/改稿时】AI 味负例注入（必须）**

```
修复/改稿是「局部手术」，与整章创作不同：模型上下文里只有原文+指出的句子，
无负例约束时，新写的句子会退回训练语料最高频的 AI 味表达——这正是「每次修复
新句都容易出 AI 味」的根因（用户 2026-08-16 观察）。修复动作必须：

1. 修复前：过一遍 `references/ai-flavor-words.md`（AI 味用词句式负例清单），
   新写的句子规避清单中的词/句式。
2. 修复后：扫【改过的位置】——词级黑名单逐词过 + 句式级模式逐条对照，
   再跑机检（项目 .check_chapter.py + 技能 check_error_cases.py）。
3. 改词联动：改动一词后检查相邻对话的呼应词（A 句改「规矩」，B 句不能还接「正经」）。
4. 踩到新坑：往 ai-flavor-words.md 对应分类追加（错误→正确），同步记项目日志。
```

**5.5 引用触发检查（每段写完必做）**

```
【检查】当前场景是否符合章节计划中定义的引用触发点？

如果符合：
  1. 调用 WebSearch 搜索相关名言/典故/诗词
     - 搜索关键词从触发点获取
     - 搜索2-3个结果，选最合适的一个
  
  2. 将引用「化用」进文本：
     ✅ 好的化用：用意境，不直接抄
     ❌ 坏的引用：生硬插入，打断节奏
  
  3. 检查引用是否自然：
     - 是否符合角色的知识背景和引用偏好？
     - 是否打断叙事节奏？
     - 如果生硬，放弃引用

如果不符合：
  - 不引用，继续创作

【重要】宁缺毋滥，找不到合适的就不引用。
```

**5.6 流行文化引用触发检查（每段写完必做，非严肃文学适用）**

```
【检查】当前场景是否符合章节计划中定义的流行文化引用触发点？

如果符合：
  1. 调用 WebSearch 搜索相关热梗/流行语
     - 搜索关键词从触发点获取
     - 搜索"最近流行梗"、"网络热梗 <年份>"
     - 选最合适的一个
  
  2. 将引用「自然化用」进文本：
     ✅ 好的化用：角色内心吐槽、对话中自然带出
     ❌ 坏的引用：生硬插入、为了玩梗而玩梗
  
  3. 检查是否符合角色身份：
     - 古代角色不会用现代梗
     - 严肃角色不会随意玩梗
     - 如果不符合，放弃引用

如果不符合：
  - 不引用，继续创作

【重要】宁缺毋滥，找不到合适的就不引用。
```

**5.7 执行负面约束检查（逐条）**

```
□ 是否出现精确数字？ → 替换为模糊表达
□ 是否直白点题？ → 删除或改写为间接体现
□ 是否重复前章用词？ → 替换同义词
□ 段落长度是否均匀分布？ → 有意混合短段和长段
□ 是否使用水字数过渡词？ → 删除，用场景切换代替
```

**5.8 输出格式**

正文输出前必须附加"创作说明"：

```
> 【创作说明】
> - 行文风格：<节奏分布>，开头方式<X>，结尾方式<Y>
> - 创作重点：<偏向>，内容比重<说明>
> - 情绪基调：<主情绪>，情绪变化<起始→结尾>
> - 负面约束检查：精确数字<N>处（已模糊化）/ 点题检查：已移除<N>处
> - 设定点引用：<设定点名>使用角度<X>（改编后）
> - 引用记录：<如有引用，注明触发点、搜索关键词、选用内容、化用方式>
> - 流行文化引用记录：<如有引用，注明触发点、搜索关键词、选用热梗/流行语、化用方式>
```

---

### 阶段四：质量检测与迭代

#### 步骤8：章节自检

自动检测本章的AI痕迹，生成自检报告：

**检测项目**：
1. 精确数字检测
2. 直白点题检测
3. 水字数过渡词检测
4. 段落长度均匀度检测
5. 用词重复检测（跨章）
6. 情绪一致性检测
7. 引用质量检查
   - 检查引用是否自然（不生硬）
   - 检查引用是否符合角色背景
   - 检查引用是否重复（跨章）
8. 流行文化引用质量检查（非严肃文学）
   - 检查流行文化引用是否自然（不生硬、不为了玩梗而玩梗）
   - 检查流行文化引用是否符合角色身份（古代角色不会用现代梗）
   - 检查流行文化引用是否符合场景氛围（悲伤场景不适合搞笑梗）
   - 检查流行文化引用是否重复（跨章）

**输出**：自检报告文件 `tracker/selfcheck-chXXX.md`

#### 步骤8.5：逐句语义通读（强制，机检之前执行）

> **为什么需要这一步？** 项目实测（2026-07-30《某书》某章）暴露：机器检查（字数/说·道/禁词/相邻重复/破折号）全清的章节，仍可能充斥**读不通的句子**——病句搭配、指代悬空、同一场面动作归属拧巴、视角跳+冗余 tell。根因是模型优化机检达标却缺乏逐句语义通读，且逐句编辑引入新不通不回读全章。**机检通过 ≠ 可读**，故须在执行机检前先过本关。对应错误案例 **#069**。

**操作（对本章正文逐句执行）**：

1. 将正文按句号/问号/感叹号/分号切句（引号内完整句也算一句），逐句**朗读式通读并理解含义**，不跳行。
2. 对每句逐项自问（任一"否"即记为问题句，定位行号）：
   - ① 语法成立？主谓宾 / 长串「的」定语归属无歧义？
   - ② 含"得"的因果/感官串（X得Y）：X、Y 属性同类、因果顺？（跨感官"亮得发烫"类硬串一律改单属性）
   - ③ 代词/通称（他 / 这后生 / 弟子）前溯 2 句有唯一明确先行词？无则补。
   - ④ 读者 3 秒内读懂、无"看不懂"卡点？
   - ⑤ 同一场面动作归属 / 视角前后一致？（无"主角堵→反派堵→又抬头见是他"式主语打架或重复见人）
   - ⑥ 是否越过当前视角角色应知信息（全知 tell / 剧透角色尚不知的真相）？
   - ⑦ 是否与紧邻上下句重复同一信息（show 完又 tell / 冗余总结）？
3. 问题句**当场重写**（或合并/删除），不得留过；改写后回读该句及相邻 1–2 句确认通顺。
4. 输出：在 `tracker/selfcheck-chXXX.md` 单列「逐句通读问题清单」表（行号 | 原句 | 问题类型 | 改法）。

**关联**：本流程对应错误案例 **#069**（机检通过但读不通的句子）；与 #005/#034/#037/#044/#051 互补，是其缝隙的兜底性总入口。

#### 步骤8.6：章节结构分层归纳（自检后必须输出）

> **为什么需要这一步？** 机器检查（步骤8 + 项目 `.check_chapter.py`）只能验证"字数与禁词对不对"，无法回答"这一章讲了哪几件事、结构松不松"。项目实测（《某书》某章 审阅）暴露：一章若直接铺成几十个等长自然段，作者/评审很难一眼抓住主线。把全章按**两层归纳**重排后，结构清晰度立刻提升，也便于后续修订时精准定位行号。故定为章节自检的标配交付物。

**操作（对本章正文执行）**：

1. 通读全章，按**内容主题**把自然段合并为"内容块"（每块是一段连续叙事 / 一个场景 / 一场对话），标注起止行号 `L起–L止`。
2. 再把内容块按**章级大阶段**归并到**第一层**——严格 **≤5 条**（理想 3–4 条）；若超 5 条，提示"本章幕过多 / 该拆章"。
3. 输出格式（固定，markdown 两级列表）：

```markdown
## 第一层：<章名> 大阶段（≤5 条）

**一、<大阶段名>（L起–L止）**
- 1.1 <内容块概要>　`L起–L止`
- 1.2 <内容块概要>　`L起–L止`

**二、<大阶段名>（L起–L止）**
- 2.1 <内容块概要>　`L起–L止`
```

**行号规则**：行号取文件实际行号（含空行）；正文段为奇数行、空行为偶数行时，内容块区间取首个正文行到末个正文行。

**配套自检价值**：
- 第一层条数 >5 → 提示"本章幕过多 / 该拆章"。
- 子层某块行号跨度过大（如单块 >25 行且无场景切换）→ 提示"该块可能空转 / 需切分"。
- 与步骤8.5（逐句通读）互补：8.5 管"每句读不通"，本步管"全章结构松紧"。

**关联**：输出可并入 `tracker/selfcheck-chXXX.md`，作为结构自检段。

#### 步骤9：读者反馈处理

将读者反馈存入追踪器，**按类型分类**：

- **AI痕迹类**：重复/节奏单一/用词单调/精确数字/点题明显
- **剧情类**：逻辑漏洞/人物OOC/节奏问题
- **其他**：文笔/设定/...

**AI痕迹类反馈必须触发对应设定的修订**，并在下一章创作计划中重点针对性改进。

---

## 关键原则（必须遵守）

1. **设定不全，不写正文**：正文创作前必须完成基础设定
2. **范例库不全，不写正文**：主要设定点必须有写作范例
3. **情绪词库不全，不写正文**：主要角色必须有情绪词库
4. **计划不全，不写正文**：章节计划三要素（行文风格/创作重点/情绪）缺一不可
5. **情绪词库是用词人感的核心**：每次更新角色设定时必须同步更新情绪词库
6. **设定点范例是点题的唯一合法方式**：禁止自创点题方式，必须从范例库选取改编
7. **负面约束优先于正面要求**：当"写什么"和"不写什么"冲突时，优先遵守"不写什么"
8. **模糊数字优先**：默认使用模糊数字，精确数字需要正当理由
9. **情绪必须贯穿全文**：从章节计划到正文创作到自检，情绪基调必须全程可追溯
10. **引用触发机制**：不在本地建素材库，创作时实时搜索，宁缺毋滥
11. **引用必须符合角色背景**：引用前必须检查角色的引用偏好和知识背景
12. **流行文化引用必须符合角色身份**：古代角色不会用现代梗，严肃角色不会随意玩梗
13. **流行文化引用必须符合场景氛围**：悲伤场景不适合搞笑梗，战斗场景不适合玩梗
14. **流行文化引用必须自然**：不能为了玩梗而玩梗，宁缺毋滥
15. **设定-情节耦合检查必须 pass**：阶段一·五不通过，不能进入章节计划阶段
16. **迭代深化必须执行**：至少2轮，不达标必须修正设定
17. **耦合检查报告必须输出**：作为项目文件保存，全程可追溯
18. **开写前必查错误案例库**：`references/error-cases/_index.md` 收录了已踩过的真实文风/设定坑（信息空转、章尾拖尾、设定一致性、专名 canon、别扭称呼、看不懂的因果句、声口趋同、长文过度修辞/无信息量总结(#064)、群像段注水/角色不讨喜/时间线矛盾/生僻词(#065)、叙述回顾段时间词(#111)/叙述语言口语化·调性断层(#113)/plan对话人物维度·时空认知五要素(#114)/补字数稀释主线·主线占比≥60-70%(#115)/分段梗概·段落+篇幅锁定(#116)/跟踪戏出场位置迭代(#117)/弹幕梗callback复用·禁复读(#118)/信息原句复读·跨位置去重·紧迫感不靠念时间(#119)等）。查章流程：先读 `_index.md` 一眼看全当前全部条目（编号至 #119）→ 跑 `check_error_cases.py <章节>` 自动扫通用机检项（#020/#023/#024）→ 跑项目 `.check_chapter.py` 扫项目级项（#007 某线亮起/#060 现代词 禁词等）→ 对「人工」项按分类文件逐条对照。遇到新问题追加新案例并同步索引。
19. **专名/设定前置对齐**：动笔前核对角色卡出身、地名、势力名，建立跨章一致的 canon 清单，避免「主角对自己门派用陌生口吻」「前后章镇名打架」类硬伤
20. **角色对话必须有声口差异**：每个角色必须有自己的说话风格（句子长度、口头习惯、措辞偏好）。不同角色不能共享同一个旁白语感。创作前必须对照角色声口特征写对话，写完后做"声口盲测"（拿掉角色名能认出谁在说话）
21. **逐句语义通读是自检硬关**：机检（字数/说·道/禁词/破折号）通过不等于可读。每章交付前必须按步骤8.5逐句读并理解含义，病句/指代空/场面归属拧/视角跳/冗余tell 一律记为问题句重写；逐句编辑后须回读全章（对照 #069）
22. **章节结构分层归纳是自检标配交付**：每次完成机检 + 逐句通读后，必须按步骤8.6输出"两层归纳表"（第一层章级大阶段 ≤5 条，子层内容块挂行号 `L起–L止`），便于作者/评审一眼抓主线、定位修订。第一层 >5 条即提示"本章幕过多 / 该拆章"。

---

## 快速参考：AI痕迹根治检查清单

每次创作前快速检查：

- [ ] **目标平台/读者调性已确定**（直给型 vs 铺陈型）并写入书核与本章 plan「要素①」标注（#104：直给平台节奏慢/不好笑=注意力分散，非情节问题）
- [ ] 设定-情节耦合检查已通过（阶段一·五）
- [ ] 耦合检查报告已输出
- [ ] 设定必要性检查已通过
- [ ] 类型-specific 检查已通过（根据小说类型）
- [ ] 逻辑自洽性检查已通过
- [ ] 迭代深化已完成（至少2轮）
- [ ] 章节计划有三要素（行文风格/创作重点/情绪）
- [ ] 负面约束清单已加载
- [ ] 设定点范例已准备（如需点题）
- [ ] 角色情绪词库已加载
- [ ] 角色引用偏好已加载（如需引用）
- [ ] 前章开头方式已检查（确保不同）
- [ ] 前3章高频词已提取（确保不重复）
- [ ] **错误案例库已对照**（`references/error-cases/_index.md` 一眼看全当前全部条目（编号至 #119）；`check_error_cases.py <章节>` 已跑通用机检 #020/#023/#024；项目 `.check_chapter.py` 已跑项目级 #007/#060 等；「人工」项按 `cat-*.md` 逐条对照：信息空转/章尾拖尾/设定一致性/专名 canon/别扭称呼/看不懂因果句/声口趋同/长文过度修辞·无信息量总结(#064)/群像段注水·角色不讨喜·时间线矛盾·生僻词(#065)/叙述回顾段时间词(#111)/叙述语言口语化·调性断层(#113)/plan对话人物维度·时空认知五要素(#114)/补字数稀释主线·主线占比≥60-70%(#115)/分段梗概·段落+篇幅锁定(#116)/跟踪戏出场位置迭代(#117)/弹幕梗callback复用·禁复读(#118)/信息原句复读·跨位置去重·紧迫感不靠念时间(#119)/角色trait落地）
- [ ] **修复负例已对照**（凡本次有修复/改稿动作：`references/ai-flavor-words.md` 已过一遍 + 改过的位置已按词级黑名单/句式级模式扫过）
- [ ] **逐句语义通读已执行**（按句切分朗读式通读，病句/指代空/场面归属拧/视角跳/冗余tell 已记录重写，对照 #069）
- [ ] **章节结构分层归纳已输出**（第一层章级大阶段 ≤5 条，子层内容块挂行号 `L起–L止`，对照步骤8.6）
- [ ] **专名与设定已对齐**（角色出身、地名、势力名与 canon 清单一致，无前后矛盾）
- [ ] **角色声口特征已加载**（不同角色说话风格差异已确认，声口对比已完成）
- [ ] **角色性格 trait 已落地**（设定的标志性 trait 在本章有可观察的外显台词/行为，非只在内心里；对照 #050）
- [ ] **配角描写分层已落实**（客观可观察的「状态/行为」用第三人称旁白直写，合法且破单调；但人物「想法/心事」与「历史/背景」一律不进旁白——想法留待该角色自己对话吐露，历史留待后续对话带出；主角**不推测配角内心**（最烦写法，禁用）；对照 #054）
- [ ] **具身代入感已落实**（无漂浮感官句/指代悬空/甩尾单字，对照 #051；非主角不感知因果线，对照 #052；背景往事不用讲故式旁白倾倒，已转具身动作法 show don't tell，正解对照 #054，#053 为错误示范史）
- [ ] **情绪由声口措辞携带（非身体扫描）**：本章情绪是否通过角色为事物命名/选动词/定褒贬体现（奉天子 vs 挟天子原理），已剔除「心口/肩膀/脊梁/刺」式身体扫描（对照 #125/#074）
- [ ] 精确数字已替换为模糊表达
- [ ] 段落长度分布已检查（确保不均匀）
- [ ] 直白点题已移除
- [ ] 引用触发点已定义（如需引用）
- [ ] 引用是否符合角色背景（自检）
- [ ] 角色流行文化熟悉度已加载（非严肃文学，如需流行文化引用）
- [ ] 流行文化引用触发点已定义（非严肃文学，如需引用）
- [ ] 流行文化引用是否符合角色身份（自检）
- [ ] 流行文化引用是否符合场景氛围（自检）

---

## 模糊数字表达参考

| 精确表达 | 模糊表达 |
|---------|---------|
| 3秒、37秒、5分钟 | 片刻、刹那、瞬息、数息 |
| 8.5米、142步 | 数步、十来步、数十米、百来步 |
| 37人、142个 | 三两人、十来个、数十、百余人 |
| 85%、第37次 | 几乎、差一点、多半、勉强 |

**例外场景**（可使用精确数字）：
- 科技设定（如"37%的能量剩余"）
- 战斗倒计时（如"倒计时37秒"）
- 系统界面显示

每章例外不超过3处，且必须在章节计划中注明。

---

## 对话标签多样化参考

**禁止**：连续使用"说/道"

**替代方案**：
- 愤怒：吼、咆哮、咬牙、厉声、冷笑
- 温和：轻笑、柔声、低语、微笑
- 犹豫：吞吞吐吐、停顿、沉默、低声
- 激动：嚷、叫、跳起来、挥手
- 疲惫：无力、低沉、沙哑、叹息

每章同一标签不超过3次。

---

## 开源说明

本技能包设计为开源项目，欢迎贡献和改进。

**授权**：MIT License

**贡献指南**：
1.  Fork 本仓库
2.  创建你的特性分支
3.  提交你的改进
4.  发起 Pull Request

**问题反馈**：
- GitHub Issues：报告Bug、提出新功能建议
- 社区讨论：分享使用技巧、讨论改进方案
